# 0.0.1

### Added

- Added `build.sh` to run the webpack production build.
- Added `docker-compose/compose-env.sh` to detect `docker-compose` or `docker compose`, and whether `sudo` is required.
- Added a Traditional Chinese README (`README_zh_tw.md`).

### Changed

- Updated Docker Compose helper scripts to use the shared compose environment helper.

### Documentation

- Documented the production build command (`./build.sh`) in the bilingual README.
